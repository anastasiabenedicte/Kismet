---
import ButtonRound from "./ButtonSquare.astro";
import CollabSlide from "./CollabSlide.astro";
---

<section class="collabs-section">
  <div class="collab-text-side">
    <h1>Collab</h1>
    <h2>Interested in doing a collab with us?</h2>
  </div>

  <div class="collab-flexbox">
    <div class="collab-wrapper">
      <p class="collab-text">
        Kismet is a café in the heart of Copenhagen, known for its unique vibe
        and creative events. We love collaborating with exciting brands and
        individuals who share our values. Feel free to reach out if you have an
        idea for a collab — we only accept inquiries via email.
      </p>
    </div>

    <div class="collab-slide">
      <CollabSlide />
    </div>
  </div>
</section>

<div class="contact-toggle">
  <input type="checkbox" id="contact-toggle" hidden />
  <ButtonRound text="Contact us" variant="variant_1" id="contact-toggle" />

  <div id="contact-box" class="contact-box">
    <p>Email: kontakt@kismet.dk</p>
    <p>Instagram: @kismetcph</p>
    <p>Adresse: Adelgade, KBH K</p>
  </div>
</div>

<p class="lookingforw">We are looking forward to hear from you :)</p>

<hr />

<style>
  .collab-flexbox {
    display: flex;
    flex-direction: column-reverse;
    gap: 2rem;
    padding: 2rem;
  }

  hr {
    margin: 60px;
    height: 2px;
    border: none;
    background-color: var(--light-purple);
  }

  h1 {
    text-align: center;
  }

  h2 {
    color: var(--dark-green);
    text-align: center;
  }

  p {
    color: var(--dark-green);
  }

  .collab-text {
    max-width: 100%;
    background-color: var(--beige);
    box-shadow: 10px 10px var(--dark-green);
    margin-top: 80px;
    color: var(--dark-green);
    line-height: 30px;
    padding: 15px 15px 15px 25px;
  }

  .lookingforw {
    color: var(--dark-green);
    text-align: center;
  }

  .contact-toggle {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    margin: 2rem auto;
  }

  .contact-box {
    margin-top: 1rem;
    padding: 2rem;
    background-color: var(--beige);
    box-shadow: 10px 10px var(--dark-green);
    color: var(--beige);
    display: none;
    transition: all 0.8s ease;
  }

  .contact-box p {
    color: var(--dark-green);
  }

  #contact-toggle:checked ~ .contact-box {
    display: block;
  }

  @media (min-width: 800px) {
    hr {
      margin: 40px;
    }

    html,
    body {
      overflow-x: hidden;
      margin: 0;
      padding: 0;
    }

    .contact-toggle {
      margin: -0.5rem auto;
    }

    .collab-flexbox {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
      max-width: 1120px;
      margin: 0 auto;
      padding: 0;
      margin-top: -50px;
    }

    .collab-wrapper {
      max-width: 560px;
      margin-top: -36px;
    }

    .lookingforw {
      margin-top: 40px;
    }

    .collab-slide {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
    }

    h2 {
      margin-bottom: 100px;
    }

    .collab-text {
      text-align: left;
      margin-left: 50px;
      margin-bottom: 10px;
    }
  }
</style>
