---
import MenuCard from "./MenuCard.astro";
---

<ul><MenuCard /><MenuCard /><MenuCard /><MenuCard /><MenuCard /></ul>

<style>
  ul {
    padding: 2vh 0 0 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    grid-auto-rows: auto;
    list-style-type: none;
    gap: 20px;
  }
</style>
